# Verified project marker

The final VaultGemma notebook requires this marker when it resolves the project
root. Research planning history is intentionally not copied into the release;
the release scope and reproduction procedure are documented in
`../README.md`.
